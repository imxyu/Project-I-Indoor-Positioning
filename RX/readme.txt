ADC：
PA0~PA3,  PF4, PF6~PF10



























新建模块：

OLED(mpu6050.h)：

/***********************************
OLED显示欧拉角数据
***********************************/
void OLED_ShowAxis(float Pitch,float Roll,float Yaw)


KEY(key.h):

/***********************************
按键和拨码开关的初始化
***********************************/
void KEY_Init(void)
void BOMA_Init(void)
/***********************************
按键扫描
***********************************/
int KEY_Scan(int mode)
/***********************************
根据按键选择不同模式（题目）
***********************************/
void Mode_Switch(int mode)
	
	
PWM(pwm.h)

/***********************************
TIM4和TIM5的初始化，共8通道PWM
***********************************/
void TIM4_PWM_Init(u32 arr,u32 psc)
void TIM5_PWM_Init(u32 arr,u32 psc)

DAC(dac.h)

/***********************************
DAC1、DAC2的初始化
***********************************/
void DAC1_Init();
void DAC2_Init();
/***********************************
在主函数中分别调用一下两函数，可改变其输出电压
改变i即可（0-4096对应0-3.3V）
万用表测没毛病，用示波器测有问题……
***********************************/
DAC_SetChannel1Data(DAC_Align_12b_R, i);
DAC_SetChannel2Data(DAC_Align_12b_R, i);